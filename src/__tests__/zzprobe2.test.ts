import { it, expect } from "vitest";

// Leftover scratch file from a debugging session — sandbox blocked file deletion (rm/mv).
// Safe to delete manually: src/__tests__/zzprobe2.test.ts
it.skip("scratch file — delete me", () => {
  expect(true).toBe(true);
});
